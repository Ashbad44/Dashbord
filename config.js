// ============================================================
// CONFIG — fill this in after you deploy the Apps Script backend
// (see README.md, Step 2)
// ============================================================

const CONFIG = {
  // Paste the "Web app" URL you get after deploying Code.gs
  // Looks like: https://script.google.com/macros/s/AKfycb.../exec
  APPS_SCRIPT_URL: "",

  // The same secret key you set inside Code.gs (SHEET_KEY)
  ACCESS_KEY: "",

  // How often to auto-refresh the page data, in milliseconds
  // (5 * 60 * 1000 = every 5 minutes). Set to 0 to disable auto-refresh.
  REFRESH_INTERVAL_MS: 5 * 60 * 1000
};
